# RobotD-data

Place RobotD CSV files in this folder. `RobotD.html` scans this fixed folder on GitHub Pages and lists CSV files in a dropdown.

If the page is not hosted on a standard `github.io` URL, update `files.json` with the CSV filenames, for example:

```json
{
  "files": ["RobotD.csv"]
}
```
